{
  "name": "mockoon-control-panel",
  "version": "1.0.0",
  "description": "Mockoon Control Panel",
  "scripts": {
    "backend:dev": "npm run dev --prefix backend",
    "backend:build": "npm run build --prefix backend",
    "backend:start": "npm run start --prefix backend",
    "backend:db": "npm run start --prefix backend",

    "frontend:dev": "npm run dev --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "frontend:preview": "npm run preview --prefix frontend",
    "frontend:prepare": "npm run prepare --prefix frontend",
    "frontend:check": "npm run check --prefix frontend",
    "frontend:check:watch": "npm run check:watch --prefix frontend",
    "frontend:format": "npm run format --prefix frontend",
    "frontend:lint": "npm run lint --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
